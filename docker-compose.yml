version: '3.8'

services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"

  frontend:
    build:
      context: ./frontend
      args:
        REACT_APP_API_BASE: /api
    ports:
      - "80:80"
    depends_on:
      - backend
